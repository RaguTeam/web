# Источник данных заводится автоматически при старте — руками в UI ничего
# добавлять не нужно.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
